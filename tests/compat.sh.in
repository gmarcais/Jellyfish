nCPUs=$(grep -c '^processor' /proc/cpuinfo 2>/dev/null || sysctl -n hw.ncpu)
pref=$(basename $0 .sh)
DIR=../jellyfish
JF=$DIR/jellyfish

check () {
    cut -d\  -f 2 $1 | xargs @MD5@ | sort | diff -w $1 -
}
